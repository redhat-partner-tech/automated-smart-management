---
- name: pip list from default EE
  hosts: ansible-1.example.com
  connection: local
  gather_facts: no
  tasks:
  - name: Just get the list from default pip
    pip_package_info:
      #clients: pip
    register: pips

  - debug:
      msg: "{{ pips.packages.pip }}"
